<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Disabled State -->
    <!-- When logout is in progress, show muted gray -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <solid android:color="@color/gray_light" />
        </shape>
    </item>

    <!-- Pressed State -->
    <!-- When user is actively pressing logout button -->
    <item android:state_pressed="true" android:state_enabled="true">
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <gradient
                android:angle="135"
                android:startColor="@color/orange_dark"
                android:endColor="@color/orange_darker"
                android:type="linear" />
        </shape>
    </item>

    <!-- Normal/Enabled State -->
    <!-- Default logout button appearance -->
    <item android:state_enabled="true">
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <gradient
                android:angle="135"
                android:startColor="@color/orange_primary"
                android:endColor="@color/orange_dark"
                android:type="linear" />
        </shape>
    </item>

</selector>